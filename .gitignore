# Remove extensionless binaries.
# Taken from https://stackoverflow.com/questions/5711120/gitignore-binary-files-that-have-no-extension
# Remove extensionless binaries 1: Ignore all
*
# Remove extensionless binaries 2: Unignore all with extensions
!*.*
# Remove extensionless binaries 3: Unignore all dirs
!*/

# Gradlew is an exception to the norm and is intended to be committed to source control
!gradlew

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
protoc-gen-grpc-kotlin-1.4.1-jdk8.jar

# Environment files
*.env

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.idea/

# Go workspace file
go.work
go.work.sum

# Certs are stored in here for now, ignore em
**/certs/*
# But keep this script to generate them and their folders
!server/certs/create.sh
!.gitkeep
